language: python
python:
  - 2.7
sudo: false
cache:
  directories:
    - $HOME/buildout-cache
    - $HOME/parts/node
    - $HOME/.cache/pip
env:
  matrix:
    - CONF=travis.cfg
install:
  - pip install -r requirements.txt
  - easy_install -U zc.buildout==2.11.0
  - buildout -c $CONF annotate
  - buildout -N -t 3 -c $CONF || buildout -N -t 3 -c $CONF
script:
  - bin/code-analysis
  - bin/test -t README
after_failure:
  - bin/flake8 --version
  - bin/csslint --version
  - bin/jshint --version
  - bin/jscs --version
